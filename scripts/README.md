# Scripts

## `validate_md5sum.sh`  
- To be ran from project root. 
- Checks that the md5sum values from the `DNA_sequencing_stats.csv` file are accurate to the downloaded files
- all `.gz` files are too large for github and are not included in the repo

